# Minimal dependencies for the demo
scrapling
pandas
psycopg2
# For crawl4ai-based scraper
crawl4ai
aiohttp

# PostgreSQL (để insert từ all_data.json)
psycopg2-binary
# openai  # Optional: for LLM extraction in crawl4ai

# Postgres connection test (scrapling_demo/test.py)
psycopg[binary]
